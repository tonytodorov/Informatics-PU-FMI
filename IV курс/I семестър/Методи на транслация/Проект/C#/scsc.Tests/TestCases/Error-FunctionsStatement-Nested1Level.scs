using System;

class Program 
{	
	int func1(int param1, int param2) {
		return param1 + param2 + param3; //expectederror "Недеклариран идентификатор param3" expectederror "Неочакван тип на символ в таблицата на символите (вътрешна грешка)" expectederror "Несъвместими типове"
	}
	
	int main() // entry point
	{
		int x;
		x = func1(4,6);
		
		System.Console.Write(x);
		return 0;
	}
}