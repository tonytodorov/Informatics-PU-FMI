using System;

class Program
{	
	int main() // entry point
	{
		int i;
		
		i = 9;
		while (i>0) {
			System.Console.Write(i);
			i--;
		}
		
		rreturn 0; //expectederror "Недеклариран идентификатор rreturn" expectederror "Неочакван тип на символ в таблицата на символите (вътрешна грешка)" expectederror "Очаквам специален символ ';'"
	}
}
//expectederror "Очаквам специален символ ';'" expectederror "Очаквам специален символ '}'" expectederror "Очаквам специален символ '}'"