using System;

class Program
{	
	int main() //entry point
	{
		int i;
		i = 4;
		if (i>2)
			if (i<) //expectederror "Очаквам адитивен израз" expectederror "Несъвместими типове за сравнение"
				System.Console.Write("i is more than two and less than six");
		return 0;
	} //expectederror "Очаквам Statement"
} //expectederror "Очаквам специален символ '}'"